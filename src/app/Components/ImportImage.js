import Image from "next/image";

export default function ImportImage() {
  return (
    <div>
      <div>
        <h1>Smocking Help</h1>
        <h2>Add and image of your thread, pattern, and fabric choice. We can show you what it might look like when its done. </h2>
      </div>
      Import Image
    </div>
  );
}
